#name: Build & Publish
#on:
#  pull_request:
#    types:
#      - closed

#jobs:
#  build-deploy:
#    runs-on: ubuntu-latest

#    steps:
#      - name: Checkout 🛎
#        uses: actions/checkout@v2

#      - name: Init
#        run: make init

#      - name: Build 🏗️
#        run: make build

#      - name: Deploy to GitHub Pages 🚀
#        uses: peaceiris/actions-gh-pages@v3
#        with:
#          github_token: ${{ secrets.GITHUB_TOKEN }}
#          publish_dir: ./book
#          cname: docs.cloud.tamu.edu